import AppShell from "@/components/app-shell";import {PageHeader} from "@/components/page-header";import {prisma} from "@/lib/prisma";import {createSacrament,createStage} from "./actions";
export const dynamic="force-dynamic";
export default async function Page(){const rows=await prisma.sacrament.findMany({where:{deletedAt:null},include:{stages:{where:{deletedAt:null},orderBy:{order:"asc"}},_count:{select:{classes:true}}},orderBy:{name:"asc"}});return <AppShell current="/sacramentos"><PageHeader title="Sacramentos e etapas" description="Estrutura pastoral administrável, sem valores fixos"/><div className="dashboard-grid"><section className="card form-card"><h2 style={{font:"700 18px Georgia,serif",color:"#1a4069"}}>Novo sacramento</h2><form action={createSacrament} className="form-grid"><div className="field"><label>Nome</label><input name="name" required/></div><div className="field"><label>Descrição</label><input name="description"/></div><button className="btn btn-primary">Cadastrar sacramento</button></form></section><section className="card form-card"><h2 style={{font:"700 18px Georgia,serif",color:"#1a4069"}}>Nova etapa</h2><form action={createStage} className="form-grid"><div className="field"><label>Sacramento</label><select name="sacramentId" required>{rows.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></div><div className="field"><label>Nome da etapa</label><input name="name" required/></div><div className="field"><label>Ordem</label><input name="order" type="number" defaultValue="1"/></div><button className="btn btn-primary">Cadastrar etapa</button></form></section></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))",gap:16,marginTop:18}}>{rows.map(s=><div className="card" style={{padding:21}} key={s.id}><div style={{display:"flex",justifyContent:"space-between"}}><h2 style={{font:"700 19px Georgia,serif",margin:0,color:"#1a4069"}}>{s.name}</h2><span className="badge">{s._count.classes} turmas</span></div><p style={{fontSize:12,color:"#718092"}}>{s.description||"Sem descrição"}</p><div className="list">{s.stages.map(st=><div className="list-row" key={st.id}><span className="person-avatar">{st.order}</span><strong>{st.name}</strong></div>)}{!s.stages.length&&<div className="empty">Nenhuma etapa.</div>}</div></div>)}</div></AppShell>}
