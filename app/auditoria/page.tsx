import AppShell from "@/components/app-shell";import {PageHeader} from "@/components/page-header";import {prisma} from "@/lib/prisma";import {requireSession} from "@/lib/auth";import {formatDate} from "@/lib/format";
export const dynamic="force-dynamic";
export default async function Page(){await requireSession(["ADMIN"]);const rows=await prisma.auditLog.findMany({include:{user:true},orderBy:{createdAt:"desc"},take:100});return <AppShell current="/auditoria"><PageHeader title="Auditoria" description="Histórico imutável das operações relevantes"/><div className="card table-wrap"><table className="table"><thead><tr><th>Data e hora</th><th>Usuário</th><th>Ação</th><th>Entidade</th><th>Identificador</th><th>IP</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{formatDate(x.createdAt)} {x.createdAt.toLocaleTimeString("pt-BR")}</td><td>{x.user?.name||"Sistema"}</td><td><span className="badge blue">{x.action}</span></td><td>{x.entity}</td><td style={{fontFamily:"monospace",fontSize:11}}>{x.entityId||"—"}</td><td>{x.ip||"—"}</td></tr>)}</tbody></table>{!rows.length&&<div className="empty">Nenhum evento registrado.</div>}</div></AppShell>}
