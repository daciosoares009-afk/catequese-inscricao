import AppShell from "@/components/app-shell";import {PageHeader} from "@/components/page-header";import {prisma} from "@/lib/prisma";import {requireSession} from "@/lib/auth";
export const dynamic="force-dynamic";
export default async function Page(){await requireSession(["ADMIN"]);const[parishes,communities]=await Promise.all([prisma.parish.findMany({where:{deletedAt:null}}),prisma.community.findMany({where:{deletedAt:null},include:{parish:true}})]);return <AppShell current="/configuracoes"><PageHeader title="Configurações" description="Estrutura paroquial e integrações"/><div className="dashboard-grid"><section className="card"><div className="card-head"><h2>Paróquias</h2></div><div className="card-body list">{parishes.map(x=><div className="list-row" key={x.id}><div className="grow"><strong>{x.name}</strong><small>{x.city||"Cidade não informada"}</small></div><span className="badge">Ativa</span></div>)}</div></section><section className="card"><div className="card-head"><h2>Comunidades</h2></div><div className="card-body list">{communities.map(x=><div className="list-row" key={x.id}><div><strong>{x.name}</strong><small>{x.parish.name}</small></div></div>)}</div></section></div><section className="card form-card" style={{marginTop:18}}><h2 style={{font:"700 18px Georgia,serif",color:"#1a4069"}}>Integrações</h2><div className="list"><div className="list-row"><div className="grow"><strong>WhatsApp</strong><small>Modo manual ativo — o envio sempre exige confirmação humana.</small></div><span className="badge warn">Manual</span></div><div className="list-row"><div className="grow"><strong>Armazenamento de documentos</strong><small>Prepare um serviço S3 compatível antes de habilitar uploads em produção.</small></div><span className="badge blue">Preparado</span></div></div></section></AppShell>}
